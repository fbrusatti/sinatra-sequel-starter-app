<!DOCTYPE html>

<html>
    <head>
        <title>
          Carreras
        </title>
    </head>

    <body>
      <main>
        <form class="box" action="/create_career" method="post">
          <label for="career-name">Nombre:</label>
          <input id="career-name" type="text" name="name" placeholder=" " />

          <button type="submit" class="primary">
            Crear
          </button>
        </form>

      <div class="box careers-box">
        Carreras:
        <ul>
          <% @careers.each do |career| %>
            <li>
              <%= career.name %>
            </li>
          <% end %>
        </ul>
      </div>
      </main>
    </body>
</html>

<style>
  body {
    background-color: #E5E7EB;
    width: 100vh;
    height: 100vh;

    margin: 0;
    padding: 0;

    font-family: 'arial';
  }

  main {
    display: flex;
    flex-direction: row;

    justify-content: space-between;
  }

  button {
    margin: 0.5rem;
    border-radius: 1rem;
    padding: 0.5rem 1rem;

    border: 1px solid;

    cursor: pointer;
  }

  input {
    border-radius: 0.5rem;

    margin: 0.5rem;
    padding: 0.5rem;

    border: 1px solid
  }

  .primary {
    background-color: #111827;
    color: white;
  }

  form {
    width: 40%;
  }

  .box {
    margin: 1rem;
    padding: 1rem;

    border-radius: 10px;

    background-color: #D1D5DB;

    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .careers-box {
    width: 55%;
  }
</style>

